/* Header guard to prevent the contents be included greater
than once during compilation.*/
#ifndef COLOR_H /* Next time compiler sees this file, will skip to #endif*/
#define COLOR_H

void setForeground(char * color); /* Function prototype*/
void setBackground(char * color);

#endif

