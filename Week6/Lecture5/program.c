#include<stdio.h>
#include<stdlib.h>

int main(int argc, char * argv[]) {

    // 1st argument = file name
    //2nd argument = mode: reading, writing, appending 
    FILE * fp = fopen("data.txt", "r");
    
    
    return 0;
}