/**
 * @file macros.c
 * @brief Defines basic macros. 
 */
#ifndef macros_h /*header guard to prevent the file from being included multiple times in compilation.*/ 
#define macros_h

#define FALSE 0 /* Define a macro for false. */
#define TRUE 1

#define FALSE 0
#define TRUE 1

/* 4. Functions to Macro */
#define BETWEEN(low, high, val) (((val) >= (low)) && ((val) <= (high)))

#endif /* End of the MACROS_H header guard */ 