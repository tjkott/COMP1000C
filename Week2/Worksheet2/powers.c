/* 
*/
#include <stdio.h>

int powersInc(void)
{
    static int power =1;
    int result;

    if (pow < halfMaxInt)
    {
        pow *= 2;
        result = pow;
    }
    else
    {
        result = pow;
        printf("Doing function will cause overflow.\n");
        #ifdef DEBUG
        
    }
}